class Display{


  element(element){
    this.answer = [];

        this.answer.push(element);
        console.log(this.answer);

        return this.answer;
    }

    
}
let display = new Display;
export default display;